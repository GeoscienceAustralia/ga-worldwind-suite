<?xml version="1.0" ?>

<project name="WorldWindRad" default="run">

	<property name="src.dir" value="${basedir}/src" />
	<property name="lib.dir" value="${basedir}/lib" />
	<property name="build.dir" value="${basedir}/build" />
	<property name="webstart.dir" value="${basedir}/webstart" />
	<property name="executable.dir" value="${basedir}/executable" />
	<property name="target.jar" value="application.jar" />
	<property name="keystore" value="${basedir}/keystore" />
	<property name="javac.debug" value="false" />
	<property name="jnlp.file" value="${basedir}/webstart.jnlp" />

	<path id="classpath">
		<fileset dir="${lib.dir}">
			<include name="**/*.jar" />
		</fileset>
	</path>

	<target name="clean">
		<delete dir="${build.dir}" />
		<delete dir="${webstart.dir}" />
		<delete dir="${executable.dir}" />
	</target>

	<target name="compile">
		<mkdir dir="${build.dir}" />
		<copy todir="${build.dir}">
			<fileset dir="${src.dir}">
				<exclude name="**/*.java" />
			</fileset>
		</copy>
		<javac source="1.5" target="1.5" srcdir="${src.dir}" destdir="${build.dir}" classpathref="classpath" listfiles="no" fork="true" debug="${javac.debug}" />
	</target>

	<target name="package" depends="compile">
		<mkdir dir="${webstart.dir}" />
		<jar destfile="${webstart.dir}/${target.jar}" basedir="${build.dir}" />
		<copy file="${jnlp.file}" todir="${webstart.dir}" />
	</target>

	<target name="sign" depends="package">
		<signjar jar="${webstart.dir}/${target.jar}" keystore="${keystore}" alias="myself" storepass="password" />
	</target>

	<target name="run" depends="sign" />

	<patternset id="manifest.exclude">
		<exclude name="META-INF/*.DSA" />
		<exclude name="META-INF/*.RSA" />
		<exclude name="META-INF/*.SF" />
		<exclude name="META-INF/MANIFEST.MF" />
		<exclude name="META-INF/INDEX.LIST" />
	</patternset>


	<target name="executable" depends="compile">
		<mkdir dir="${executable.dir}" />
		<zip destfile="${executable.dir}/temp.zip">
			<fileset dir="${lib.dir}" includes="jogl/**" />
			<zipgroupfileset dir="${lib.dir}">
				<include name="**/*.jar" />
			</zipgroupfileset>
		</zip>
		<jar destfile="${executable.dir}/${target.jar}">
			<fileset dir="${build.dir}" />
			<zipfileset src="${executable.dir}/temp.zip">
				<patternset refid="manifest.exclude" />
			</zipfileset>
			<manifest>
				<attribute name="Main-Class" value="application.Application" />
			</manifest>
		</jar>
		<delete file="${executable.dir}/temp.zip" />
	</target>
</project>
